# 更新日志

## 开发中（下一版本）

### 新增功能

* 新增运行日志：启动、任务、删除等关键操作独立留存 7 天，任务记录与成片产物被清除后仍能回溯

* 配置框表头「预检测结果」右侧新增刷新按钮，一键按预缓存方式刷新（增量、不重置）

* 设置页「提取前缀」支持多值：回车添加，可拖拽调整顺序、点击删除；多个前缀命中时按设置顺序全部写入成片名

### 修复

* 修复同名素材被覆盖后预检测仍报不合格：缓存指纹加入文件大小 —— 覆盖操作会保留源文件的修改时间，此前只比时间戳便判定「素材没变」；内存缓存同样改为按指纹校验

* 修复批量任务失败或中断后无法续跑：无失败记录时也能按已产出成片反推出缺失序号，「继续制作」不再提示找不到可续跑的成片

* 修复打开设置页报「读取设置失败」、设置只加载到一半

* 修复设置页已保存的提取前缀不显示，新输入的前缀保存后丢失

* 修复部分删除操作未移入回收站：改为始终走回收站可还原（涉及「重新开始」清理旧产物、遮罩成片与次级产物删除等）

### 优化

* 任务执行引擎收敛为内置引擎，不再依赖系统安装 PowerShell

* 系统提示音改用系统原生方式播放，不再调用 PowerShell —— 至此运行时对 PowerShell 的依赖彻底归零

* 清理旧版遗留的无效配置项（脚本目录、引擎开关），配置文件不再携带已废弃字段

* 等宽字体换用 JetBrains Mono 官方标准版并补齐 OFL 许可，字体体积减至约原来的 1/6（-2.1 MB）

* 字体目录扁平化为 `frontend\fonts`；品牌字体 Cinzel 移出皮肤目录，消除重复字体声明

* 续跑时先声明「续跑模式与范围」再报总数，不再让「共 N 个」被误读为要重做全部

* 刷新预缓存的结果提示改为「本次更新 X 个视频，共 Y 个视频」

* 设置页折叠态摘要不再展开具体项内容；成片名预览用「前缀」占位，不再展开实际值（实际命中要到运行时才判定）

## 2026-09-17（v2.1.0）

### 新增功能

* 确认类操作改为贴着相关按钮弹出的气泡提示，不再弹出居中大窗

* 操作结果与失败提示改为角落轻提示，比状态栏小字更醒目

* 不可用的选项与按钮点击时会直接说明原因，不再只靠鼠标悬浮提示

* 任务成片已不存在时，任务列表以「已删除」标注该任务

* 设置页新增「维护」区：重建预检测缓存

### 修复

* 修复清除或删除任务时可能误删同一天其他同名任务的成片（同一分钟内重跑的配置尤其容易受影响）

* 修复外部配置识别不准：不再仅凭文件名判断，改按成片文件夹归属识别

* 修复移除日期分支未走回收站

* 修复新任务编号可能受历史异常数据影响

### 优化

* 「刷新预缓存」合并为单一动作（更新变化的视频并顺带清理失效缓存），全量重建移入设置页「维护」

* 移除左侧菜单中重复的模式切换项与遮罩模式下重复的退出按钮（顶部模式切换已承担）

* 成片数量默认值改为占位符展示，与分组数一致，留空即使用默认值

* 同一操作结果不再同时显示状态栏小字与提示

## 2026-09-14（v2.0.0）

### 新增功能

* 批量任务支持「继续制作」：失败 / 中断后仅补做失败成片，成片序号、分组后缀、输出目录与首次一致，拼接日志追加而非覆盖

### 修复

* 修复批量任务偶发「提交即失败」：沿用状态误用定长数组，越界写入直接抛异常

* 修复部分素材时长完全一致时，重试反复做无效替换并最终重复使用同一片段

* 修复复刻缺失片段找回时可能选中同源同名文件，导致成片内出现重复片段

* 修复复刻「按文件名找回片段」路径因脚本语法问题直接报错退出（整批不出片）

* 修复复刻读取视频缓存永不命中（时间戳基准错误）导致的重复探测

* 修复批量任务续跑在分组配置下无法命中：成片名带分组后缀（如 `…-2A.mp4`）时解析不出序号，续跑直接报错退出

* 修复批量任务续跑找不到配置或补做出来的成片改名：配置正本已随成片归档，若改用归档路径会让成片名多出一段目录名而与首次不一致

* 修复刷新预缓存后素材行长时间停在「检测中…」：缓存失效清理在主线程逐条查文件，数千条时阻塞界面响应

### 优化

* 批量任务执行中现场探测到的新素材会写回缓存，后续预检测与任务启动更快

* 任务排队时统一显示「等待获取互斥锁」状态（批量 / 遮罩 / 复刻口径一致）

* 底层任务执行引擎重构为 Node 引擎（遮罩 / 复刻 / 批量三个任务已迁移并通过与脚本的对照验收），为后续移除 PowerShell 运行时依赖做准备

* 任务执行引擎正式切换为内置 Node 引擎：引擎随包分发，不再依赖系统安装 PowerShell；旧脚本保留为一键回退通道

* 视频信息缓存改用内置数据库存储：增量写入、时间戳精确无损，并与任务引擎共享同一份数据

* 缓存失效清理改为「本轮已扫描到的路径免查盘 + 分批让路」，数千条缓存下的界面停顿由秒级降为毫秒级

* 配置路径检测的快捷方式解析增加结果缓存并移出主线程，检测过程不再卡顿


## 2026-09-14（v1.9.0）

### 新增功能

* 升级 Electron 44 应用内核，构建精简为中文 / 英文两种语言包

* 批量 / 复刻重试升级为渐进替换：保留有效片段、仅替换最长片段，失败按实际中断点明确提示

* 遮罩叠加新增水印透明度设置；素材命名口径统一为「原片 / 遮罩」

### 修复

* 修复日志搜索定位错日期分支（按目标日志文件精确跳转）

* 修复复刻同日连续生成文件被覆盖（自动序号化命名）

* 修复配置中同名路径重复导致的过度排除与成片重复片段

### 优化

* 数字输入框取消步进控件，聚焦时滚轮不再误改数值


## 2026-09-09（v1.8.0）

### 新增功能

* 支持浏览器访问：应用内置 HTTP 服务，浏览器与本体功能一致，托盘可一键在浏览器中打开

* 设置页新增访问令牌管理（固定令牌、随机生成、可点击访问链接）

* 遮罩与原片时长校验：时差超过 1 秒或时长未知时禁止开始制作（选择与提交双重拦截）

* 复刻任务优化：标题规范为「项目名/日期-名称/模式」并标注续跑；当日多次去重复刻自动序号化命名

### 修复

* 修复批量模式无法切换日期分支

* 修复应用内更新日志 / 说明文档无法加载（打包白名单遗漏文档文件）

* 修复复刻定位日志 / 打开成片文件夹指向错误目录（改为提交日期产物目录）

* 修复设置窗口标题栏与关闭按钮与主窗口样式不一致


## 2026-09-08（v1.7.0）

### 新增功能

* 新增「遮罩叠加」模式：多原片 × 多遮罩组合生成（遮罩+水印 / 仅水印 / 仅遮罩），GPU 编码、断点续跑、独立日志与成片管理

* 任务行右键新增「定位」，可定位至对应配置或日志（支持 -N 序号分支）

* 复刻任务失败成片支持「继续制作」断点续跑；清除任务时同步清理对应复刻日志

* 开机自启动静默化（后台托盘运行，不弹前台窗口）；冷启动提速（按需扫描 + 增量缓存构建）

### 修复

* 修复成片搜索跳转到错误配置 / 日志（同名配置过滤，含外部「*」配置）



### 优化

* 检查更新不再写缓存


## 2026-09-02（v1.6.0）

### 新增功能

* 项目列表新增「新增配置」按钮，可直接在项目下新建空白配置

* 路径行支持右键插入，修改路径独立成凭据按钮

* 日期分支支持右键移除（可选删除范围，自动清理空目录）

* 项目名右键新增「打开项目位置」

* 新增每日定时检查更新（可设定检查时刻）

* 新增更新方式设置：发现新版本后「自动检查并下载」或「仅提醒」（对启动时、定时、手动检查均生效）

### 修复

* 修复皮肤切换名片重复的问题

* 修复安装版更新检查误报「Release 缺少便携包」的问题

* 修复日志搜索跳转错误的问题

### 优化

* 移除水印文件悬浮预览小窗

## 2026-08-31（v1.5.2）

### 新增功能

* 关闭主窗口行为可选（退出 / 最小化到托盘），支持「不再询问」

* 托盘菜单自绘并随皮肤；任务支持「重新开始」，任务模式徽章分色

* 配置改动自愈（写入即刷新、前端统一入口 + 低频兜底）；未保存修改离开提醒（三选）

* 预检测支持后台运行与取消（缓存原子替换）；日志高亮块自动置顶、任务日志展示精简

* 主流水印设置（配置右键入口，「保存并替换」可递归改写项目内全部水印）

* 已完成列表右键重分组；更换水印默认打开上一个水印目录；设置页直达配置保存目录

* 快捷方式素材目录预检测支持

### 修复

* 修复任务管理器应用名 / 图标异常、已完成列表路径与跨日分组排序、水印共用误弹窗等问题

### 优化

* 弹窗按钮语义色规范化、日志滚动定位优化

* 移除「灰橙」皮肤与全局「水印目录」设置

## 2026-08-31（v1.5.0）

### 新增功能

* 预检测支持后台运行，可配合开机自启（静默到托盘）随登录常驻，减少首启等待

* 成片索引改后台分批重建，冷启动与扫描明显提速

* 日志预览新增简化模式（默认），成片块整块高亮显示

* 水印支持归属校验与跨项目保存，文件名悬浮可预览水印图片

* 任务列表：已完成/已停止支持分级清除（移入回收站）；ffmpeg 实时进度折叠为单行、日志字段中文化、显示任务总用时

* 日志滚动智能跟随，手动上翻后自动暂停跟随

* 成片分组命名改为生成时即时确定；片段路径匹配规则扩展（不区分大小写）

* 空配置自动清理、侧栏「共 N 个配置」计数实时同步、路径与排除字段支持软删除

### 修复

* 修复已删除配置残留、日志分组头错位、复刻项目外部编辑等若干问题

### 优化

* 外观与皮肤整体收敛、状态栏排布优化

## 2026-08-27（v1.4.7）

### 新增功能

* 新增「深海女仆」主题皮肤（母本移植，JS 注入动态渲染）

* 接入码云（Gitee）更新源，更新下载增加加速地址候选链

* 应用内自制标题栏；预览面板折叠重构（折叠按钮接管调宽）

* 左侧栏置顶按钮；项目展开 / 切换的徽章动画区分

### 修复

* 修复带序号后缀配置的时间排序问题等

### 优化

* 外观细节统一适配皮肤

## 2026-08-26（v1.4.6）

### 新增功能

* 更新下载阶段状态实时同步（连接 / 下载 / 校验 / 完成）

### 修复

* 任务置顶位置、已完成任务分组头交互修正

## 2026-08-26（v1.4.5）

### 新增功能

* 项目列表按首字母 / 月份分组标签；项目行徽章与最近日期

* 已完成任务按日期分组展示

* azbar 高亮自适应；安装版更新改用 electron-updater

### 修复

* 修复安装版更新失效、时间排序「0月」分组、配置表头错位等

### 优化

* 预检测结果文案、设置页说明精简

## 2026-08-26（v1.4.3）

### 新增功能

* 自动更新（便携 / 安装双通道，断点续传 + 校验 + 重试）

* 首次配置引导窗口；配置保存位置切换（含 Cache 迁移）与启动自动仲裁

* 左侧菜单按钮、配置去重、未保存退出确认、选择路径两步检测

* 设置页新增例行信息入口

### 修复

* 修复预检测缓存失效、图标缺失、设置选项冲突等问题

### 优化

* 更新检查失败分类提示、发布资产命名统一

## 2026-08-25（v1.3）

### 新增功能

* 日志分支序号（-1/-2/正本 / 外部 `*`）与对应日志精确配对

* 成片脚本内置化，脚本缓存与参数统一由应用管理

* 成片名搜索索引缓存，冷启动搜索大幅提速

* 运行环境缺失硬拦截；保存改「覆盖当前配置」+「保存为当日配置」

* 任务窗口重构（三标签页、持久化、队列拖拽、全部继续/暂停）

* 项目展开 / 折叠动画；日志成片行右键菜单（打开 / 打开所在文件夹）

### 修复

* 修复日志日期分支聚合、配置↔日志跳转配对、暂停恢复顺序等问题

### 优化

* 三组切换按钮视觉统一、任务按钮计数完善

## 2026-08-24（v1.1 · v1.2）

### 新增功能

* 单实例锁（跨盘 / 跨版本仅允许一个实例）

* 独立设置页面：通用 / 拼接 / 复刻三类参数集中管理

* 脚本关键参数可配置化，启动前参数校验

* 系统托盘与多分辨率图标

* 重置预检测、日期分支右键菜单

* 日志批量复刻（选择模式）、配置↔日志精确配对、复刻独立项目、日志多日期分支

### 修复

* 修复任务占位显示、单成片进度误判、日志归属错位、重新检测状态不结束等

### 优化

* 成片实时进度、预检测提速、缓存统一、路径悬浮显示等

## 2026-08-23（v1.0）

### 新增功能

* 任务列表窗口（互斥锁状态、实时日志、暂停/继续）

* 任务失败原因红字展示

* A-Z 纵向索引条、三套皮肤切换

* 日志全局搜索、联动预览、片段存在性预检测

### 修复

* 修复日期分支排序、添加路径、脚本启动乱码等早期问题

### 优化

* 缓存指纹失效机制、路径解析健壮性

## 2026-08-20（v0.1.0）

### 新增功能

* 首个版本：项目 / TXT 扫描、配置与日志解析、成片预检测、复刻双模式、批量生成

* 迁移至 Electron 架构；项目更名 Video Lab

* 首次运行配置引导；「外部编辑」「选择路径」等基础能力

### 修复

* 修复启动空白窗口、框选误触发、重复弹窗等问题

### 优化

* 排除字段回车添加、面板宽度拖拽、实时预览

