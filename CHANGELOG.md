# 更新日志

## 2026-08-31（v1.5.0）

### 新增功能

* 后台预检测：预检测可缩到后台执行（状态栏右侧 mini 进度 + 转圈），不阻塞主界面操作；后台大探测遇行内预检测自动让路，两者结果经内存缓存合并落盘，互不覆盖；更换工作目录自动取消并清空旧探测

* 开机后台自启动：设置项独立分组（位于「配置和数据保存位置」上方，文案「开机后台自启动（静默到托盘）」），开启后登录静默启动并驻留托盘（仅安装版生效，开发版自动跳过）

* 成片索引后台分批重建：按配置目录 mtime 增量失效，重建过程经进度事件实时更新遮罩文案，更换工作目录自动取消重建并清空旧索引

* 「刷新预缓存」二级操作：菜单按钮更名为「刷新预缓存」，点击弹窗二选一（仅刷新 / 全部重置），重置保留二次确认，弹窗带右上角圆形关闭按钮

* 日志预览简化 / 原始双模式：默认简化模式（片段路径仅显示文件名（含后缀）、隐藏行号、成片间整体分隔、成片块圆角金边容器 + 超长文字截断、片段与水印行间浅金细虚线）；原始模式完整显示原文；进入日志模式自动展开右侧栏、退出自动恢复原宽度状态；品牌名片点击强制折叠右侧栏；预览面板初始宽度调宽至 320px

* 成片块高亮重写：按「成片名 → 使用片段 → 水印」整块高亮（剔除分割线 / 空行 / 纯符号行），女仆皮肤浅蓝高亮

* 水印归属校验：选中配置预检测时校验水印是否属于本项目主流水印（位于项目目录树或全局水印目录即通过），不通过时水印框上半区浅红底 + 文案提示（红框仅保留用于用户修改提示）；归属关系固化为物理缓存（Cache\watermark\_cache.json，按工作目录隔离，更换工作目录自动重置并重建）

* 水印跨项目保存：覆盖保存 / 保存为当日配置 / 开始任务三个写盘入口自动识别水印归属项目，多项目命中弹出行式选择（原始项目 + 候选项目各一行），保存后侧栏同步刷新并在新项目内展开；任务提交时注入时间戳，脚本侧成片命名与日志日期使用提交时刻（排队跨日不漂移）

* 水印界面重构：框体分上下栏（上：水印文件名，点击即更换水印；下：配置名输入框）；文件名居中显示（不含扩展名），右侧「打开文件 / 打开文件夹」图标按钮（无 PNG 时禁用）；文件名 hover 弹出图片预览（独立置顶透明小窗、跟随鼠标右上方、按显示器边界翻转避让、约 60fps 跟随）；数字输入框去除上下箭头

* 任务清除分级：已完成 / 已停止列表清除弹窗三选（仅列表 / 列表+成片 / 全部清除），删除经系统回收站，带二次确认

* 任务进度细节：ffmpeg 进度行折叠为单行实时进度（任务结束保留最后一行），日志字段中文化（总帧数 / 质量 / 大小 / 已用时 / 速度，规则前后端统一）；任务总用时动态格式（依耗时自动 秒 / 分 / 时）固定于进度行，三段布局防止百分比与用时跳变

* 日志滚动智能跟随：内容整窗重绘并区分程序 / 用户滚动，用户上滚后暂停跟随，经恢复入口（折叠任务按钮 / 切换标签 / 关闭列表）后重新跟底

* 分组命名即时化：脚本侧单个成片生成时即计算分组字母并命名，不再全部完成后统一改名，避免误碰输出目录中的历史文件、中断后更易对账

* 提取前缀规则扩展：片段完整路径（含文件夹）不区分大小写匹配设置中的字符串即生效；配置清理后侧栏空壳配置自动移除；侧栏「共 N 个配置」徽章计数与最新日期随日期分支刷新实时同步

* 复刻项目补全：日志日期分支按日志文件名前 4 位（MMdd）渲染，外部编辑正确解析真实日志路径

* 软删除统一：路径行与排除字段删除为软删除（可恢复、右键「移除」），任一保存方式（覆盖 / 当日 / 启动任务）落地时转为硬删除

### 修复

* 已删除 / 已迁移配置残留：版本列表缓存命中分支补磁盘存在性校验，目录 mtime 指纹核对使缓存自愈；成片任务完成时自动刷新配置树

* 任务日志重复行：后端未清空实时进度行导致固化行与实时行并存

* 已完成分组头错位（重建导致首行跳位）、已停止列表残留「已完成」分组头

* 水印误报与漏判：归属判定放宽到「项目目录树或全局水印目录即通过」；更换水印后未立即重判（增加防竞态）；浅红底被女仆皮肤样式覆盖

* 预览小窗系列：消失（URL 缺斜杠 / 拦截鼠标事件）、无边框 / 底色不透明、左上角闪现、尺寸随移动持续增大（高 DPI 坐标偏移 → 改由主进程读取真实光标 + 尺寸缓存 + 降频限位）

* 复刻项目外部编辑打不开；配置清理后列表 / 日期分支残留

* D 盘 asar 与 E 盘不一致（CHANGELOG 内容差异 + run.js 换行符差异），并修复打包体积异常（依赖瘦身、Copy-Item 嵌套冗余、OutAsar 误写）

### 优化

* 字体体系：内嵌 JetBrainsMono Nerd Font（等宽）与 Cinzel（品牌，含 OFL 许可随包分发）；正文 / 等宽 / 品牌三套字体分工，计数类徽章等改用 Segoe UI；主窗口标题栏品牌名与品牌名片字体统一（其余窗口保持默认）

* 启动提速：冷启动实测约 1s（瓶颈为全量扫描与日志收集），扫描缓存启动时不重写、自动更新检查异步发起不阻塞窗口；成片索引改后台分批

* 状态栏排布：环境检查提示移至任务按钮左侧，环境正常时不显示

* 任务窗口视觉：tab 样式统一为模式切换同款（透明底米色字 / 激活金色下划线发光）、tab 间整高渐隐细金分隔线、tab 栏牛皮纸材质、任务列表多层背景 + 左下角 chibi

* 底部状态栏改为纸张材质；配置栏按钮 hover 变亮并上抬；配置栏预检测徽章胶囊（自适应宽度、主题变量配色）

* 左右面板调节方案收敛：右侧尺寸条固定全高 5px、常态隐藏 hover 浮现；左侧列表栏铆钉透明栏 + 三竖线按钮拖拽调宽

## 2026-08-27（v1.4.7）

### 新增功能

* 接入码云（Gitee）更新源：设置页「更新」新增更新源选择（码云默认 / GitHub），检查与下载自动跟随所选源；码云直连 gitee.com Release API，不经 GitHub 加速链

* 女仆主题皮肤（全新，JS 注入式的母本移植）：皮肤加载器与角色舞台（左右 + 中央女仆，随各框体出现 / 尺寸变化自适应，配置选中时收缩动画；未选中且预览展开时另有组合尺寸）；侧栏母本三层背景 + 右缘双金线框饰 + 四角金饰 + 蕾丝 / 蝴蝶结装饰衬层；中间区「landing / 对话」双态头部（navy 布料纹理 + 金色发光下划线，点击配置后头部从上往下展开，未选中配置时隐藏空态框）；品牌名片（mask 同色图标）+「共 N 个配置」亮金徽章、蓝色缎带展开填充、chibi 半透明占位、设置 / 菜单长名片（border-image 鎏金框饰）、配置项 hover 亮蓝 + 淡金边框等全套母本视觉

* 自制标题栏：去除系统边框，应用内标题栏（34px，标题与窗口控制按钮，主窗口 / 任务窗口 / 设置页顶部相应让位）

* 更新下载加速：检查更新与便携版下载优先走加速地址候选链（gh-proxy.com → gh-proxy.org → 直连兜底），失败自动回退直连，保留重试与断点续传；安装版更新源指向加速地址；加速域名集中为单一常量便于维护

* 预览面板折叠重构：移除原尺寸拖拽条，折叠按钮接管调宽（240-720px，松手记忆宽度）；右面板左缘以细亮金线分隔；展开 / 折叠 300ms 动画且按钮随面板逐帧平滑跟随（无跳变 / 回弹）

* 左侧栏置顶按钮：菜单按钮上方同风格按钮，点击平滑滚动至列表顶部

* 徽章动画场景区分：项目展开 / 收回保留动画，切换配置时保持静止

* 模式切换按钮样式统一（sort / preview 与中间模式按钮同款：透明底米色字、激活态金色下划线发光、hover 淡金浮层）

### 修复

* 时间排序下 `*-1 / *-2` 后缀配置滞留列表底部：排序键仅识别纯 4 位日期，改为提取前 4 位为主键、后缀序号细分

* D 盘 asar 丢失自动更新模块：同步时排除 node\_modules 所致，两盘内容与体积恢复一致

* 品牌图标消失 / 偏紫：`<img>` 替换元素 mask 不稳定，改 `<span>` + background/mask 方案

* 预览折叠按钮位置跳变、回弹、贴边、展开瞬间错位：改为 rAF 逐帧读取面板实时宽度驱动定位，折叠基准余量修正

* 右侧栏折叠动画中文字逐行堆叠：white-space / min-width / flex-shrink 固定布局

* 徽章文字误染投影：显式清除继承自项目名的 text-shadow

* 收回项目时徽章滑出动画不生效：折叠 / 切换路径先摘除静止类再添加滑出类

### 优化

* 标题栏高度自适应（34px）与窗口按钮对齐；女仆尺寸多状态自适应；品牌名片 / 菜单名片多次收窄（改 padding 实现真正收窄）；chibi 半透明化不遮挡文本；侧栏滚动条内移避免压金线；日期分支粗金线改细金线（与配置激活项同色）；激活态发光下划线贴文字下方；更新加速站实测定档（gh-proxy.com 最快，失效自动降级）

## 2026-08-26（v1.4.6）

### 新增功能

* 更新下载阶段状态实时同步：左下角状态栏随 连接服务器 / 首次下载 / 中断重试 / 完整性校验 / 下载中（百分比）/ 下载完成 各阶段推进更新，与任务按钮左侧下载进度条一致

### 修复

* 任务置顶后 UI 位置不变：任务卡按后端顺序始终重新排序

* 已完成任务分组头展开 / 折叠改为点击整行切换，并收紧分组头高度

## 2026-08-26（v1.4.5）

### 新增功能

* 项目列表分组标签：名称排序按首字母、时间排序按月份，在组首显示独立分组标签行（大写首字母 / N月 + 延伸细线），替代原分隔横线

* 项目行右侧信息：未展开时显示最新配置日期；展开后变为「共 N 个配置」胶囊徽章（背景色底 + 品牌蓝字，随皮肤变量），带从左侧划入 / 收回时左侧滑出的动画；复刻项目不显示徽章与日期

* 排序规则调整：配置项排序升降序仍由排序按钮控制，项目名始终保持名称升序，复刻虚拟项目固定置于列表最下方

* 已完成任务按日期分组：分组头显示「yy.mm.dd + 延伸横线 + 共 N 个视频」，点击分组头切换展开 / 折叠（默认展开最新一组），状态仅本次进程内记忆

* azbar 高亮自适应：选中的配置滚出可视区时，首字母高亮回退到列表顶部配置；配置重新进入可视区后再恢复突出该配置

* setup 安装版更新走 electron-updater 组件：检测复用 GitHub API，下载 / 安装 / 重启由 electron-updater 处理，与便携版 zip 覆盖更新双通道并行

### 修复

* setup 版自动更新失效：electron-updater 此前仅是传递依赖、未随包安装 → 显式加入依赖并随安装包提供

* app.asar 体积异常膨胀（数百 MB）：全量安装把开发依赖打入包内 → 改为以生产模式安装依赖，恢复约 3.4MB 正常体积

* 配置时间排序出现「0月」分组：月份改为从分支标签可靠提取

* 配置模式表头与数据行错位：统一列宽与间距、补齐表头占位列

* 已完成分组视频数误按任务数统计：改为读取成片总数

* 项目徽章引用不存在的颜色变量导致界面整体变蓝

### 优化

* 预检测结果文案：未检测到视频显示「无合格视频」，存在多个子组显示「xx 个视频，共 y 组」；日期分支移除「最新」角标

* 设置页「配置和数据保存位置」说明灰字仅保留目录路径（配置与 Cache 均存放于该目录）

## 2026-08-26（v1.4.3）

### 新增功能

* 自动更新（便携版 / 安装版双通道）：

  * 检查更新：应用启动自动检查、托盘右键「检查更新」、设置页入口，经版本接口对比最新版本；便携版下载便携包经校验后由外部更新器覆盖更新，安装版走安装器自动更新组件

  * 两步式更新交互：点击更新后主窗口底部出现「更新 vX」提示条（下载进度条 + 百分比），下载完成后弹「取消 / 更新并重启」确认框，确认后自动退出、覆盖、重启；提供忽略按钮

  * 下载可靠性：断点续传（Range 请求）、3 次自动重试（网络失败时续传重试）、SHA256 哈希校验（无摘要时回退字节数比对）、连接 / 下载超时中止并清理临时文件、失败保留日志

  * 更新互斥与去重：同一时刻仅允许一个下载 / 安装操作，进行中触发检查会收到提醒（含托盘入口）；已下载完成不重复下载

* 引导窗口与首次配置：新增独立引导窗口替代系统对话框；未设置工作路径时主窗口显示引导层引导选择；完成选择后自动扫描配置并执行预检测，同时自动探测有效水印目录完成水印配置

* 左侧菜单按钮：左下角新增圆形悬浮菜单按钮，点击弹出二级菜单（自下而上展开、半透明背景、浅灰边框、整框弹出动画），整合原底部操作按钮

* 配置去重：外部 `*` 配置与成片夹内正本内容完全一致时自动删除（按内容哈希判定）

* 设置页新增「配置保存位置」切换（程序目录 / 用户数据目录，保存即迁移并即时生效，仅迁移写入失败时才需重启）；后扩展为「配置和数据保存位置」：Cache 缓存目录随配置位置一并迁移（复制后删除旧目录），更新日志与任务列表持久化文件同步迁移；新增 GitHub 仓库入口；通用设置顺序调整为 主题外观 → 工作路径 → 水印目录 → 配置保存位置 → 更新

* 启动时自动仲裁配置与缓存文件位置：两处均无则进入引导窗口；两处各有一份则采用新位置；一处有配置而另一处有缓存时自动将缓存对齐到配置侧；迁移结束后自动清理源侧遗留的空目录（AppData 侧整体消失）

* 设置未保存退出二次确认：有未保存修改时点退出，在关闭按钮上方弹出确认浮层（取消 / 确认不保存）

* 选择路径两步检测：选择工作路径后先重扫配置列表，完成后再自动重置预检测并显示进度，窗口失焦时后台持续执行

* 图标单一来源：图标定义集中为单一文件，主窗口 / 任务窗口 / 设置页统一引用，杜绝图标缺失

* 任务列表行级删除按钮常态可见，样式与右下角「清除」一致（实心红底白「×」、悬浮变亮）

* 任务快照文件更名：task\_snapshot.json → task\_cache.json 并自动迁移旧文件

### 修复

* 预检测缓存明明存在却全部重测：高精度时间戳经浮点存储丢失精度 → 改为精确整数记录并以字符串持久化（兼容旧缓存），脚本侧预检测耗时由约 17s 降至约 0.2s

* 图标反复缺失：补全图标定义并由单一来源统一提供

* 设置页同名选项共用键导致选中状态被覆盖；退出时向已销毁窗口发送事件引发报错

* 未修改设置点退出时误弹确认层；点击「立即更新」后无反馈（增加即时进度事件推送）

* 引导层覆盖左下角菜单按钮：底部让位、透明背景、不拦截点击

* 固定到开始菜单的磁贴随版本更新消失：固定应用用户模型 ID

* 任意入口启动都会弹出引导窗口：用户数据目录配置中工作路径为空字符串所致，并修复设置页保存时工作路径被无条件覆盖的缺陷

### 优化

* 更新检查失败按原因分类收尾（检查失败 / 无更新 / 缺少安装包），提示与日志可追溯

* 更新下载文件名与发布资产一致；便携版与安装版更新功能做一致性核验

* exe 图标因资源管理器缓存未刷新时提供刷新指引

## 2026-08-25（v1.3）

### 新增功能

* 日志分支序号：配置版本按多个成片文件夹序号化（-1/-2…），成片夹外的单独配置加 `*` 后缀；日志日期分支按「日期 + 序号」逐条展示，成片列表按所选分支精确过滤，配置与同序号日志一一对应

* 脚本内置化：批量拼接 / 视频复刻脚本移入应用目录内的脚本文件夹并更名（video\_batch.ps1 / video\_replica.ps1），由应用固定路径调用，不再依赖外部脚本目录；设置页移除「脚本目录」相关项与首次引导

* 脚本缓存与应用缓存统一：脚本侧缓存（预检测 / 用量等）统一写入应用 Cache 目录（经环境变量注入），预检测缓存与应用共用同一文件

* 脚本默认参数脱敏：默认不携带任何个人信息（提取前缀默认为空、生产者改为通用占位），实际值由应用设置注入

* 成片名搜索索引缓存：新增专用索引（仅存成片条目，不含日志全文），按配置目录 mtime 增量失效，冷启动搜索提速约 7000 倍

* 批量拼接设置页重排：参数按成片名实际拼接顺序（创作者名 → 提取前缀 → 后缀）排列，下方为实时成片名预览（单行省略），再下为生成参数；页面分「成片命名规则 / 批量拼接参数」两个主标题区；「序号后缀」改名「后缀」且允许留空（留空时成片名不再带默认标识）

* 环境缺失硬拦截：运行环境不全（缺 pwsh / ffmpeg / ffprobe 任一）时，所有调用脚本的按钮置灰禁用并悬浮提示缺失原因，脚本调用入口带防绕过校验

* 保存配置改「覆盖当前配置」：警示红底白字，配置未修改时禁用；「保存为当日配置」始终可用；配置改动时路径行、排除字段、水印框体及配置名给出红色修改提示

* 配置路径状态样式：路径不存在显示警示橙边框与橙条，排序改变显示红底与红条，两者可叠加；输入框文字变更、排除字段增删均有对应提示，支持软删除 / 右键硬删除

* 任务窗口重构：三标签页（正在运行 / 已完成 / 已停止）带图标与任务数，已停止含报错任务时按钮浅红提醒；窗口禁止改尺寸；右下角拆分「全部继续 / 全部暂停」两个按钮；各任务行可单独清除

* 任务持久化：任务状态变化节流写盘、启动自动恢复；托盘退出时如有运行中任务弹二次确认（确认后运行中任务标记「已中断」移至已停止区、排队任务转暂停）

* 任务队列交互：暂停任务显示橙色圆点不占序号位，继续按暂停时顺位插回队列并重算序号；运行中任务置顶不可拖拽，排队/暂停任务支持拖拽排序

* 项目列表展开动画：项目名主题色从左到右填充、azbar 伸出、配置淡入三动画并行；折叠时配色收回、azbar 收回、配置区渐变消失、下方项目错峰渐显；多行动画按整列整体处理

* 日志成片行右键菜单：可打开成片 / 所在文件夹，成片缺失时置灰并悬浮提示；路径排序按钮上方新增「排序」文字提示

### 修复

* 日志列表仅显示一个日期分支：去除单日过滤限制，改为按「项目 + 配置名」聚合返回全部日志日期分支

* 切换日志日期分支后成片列表为空：成片查询改为以当前选中日期的日志文件为准，而非原配置目录

* 配置→日志跳转落点错误（原跳到最新日志）：按规则定位当日对应分支（序号对应序号、正本 / 外部 `*` 按规则对应）

* 从日志返回配置时对应不上：按当前选中的日志重新定位对应配置版本

* 日志模式副标题 title 被改回：恢复为「日志:"相对路径"」并支持悬浮查看完整路径

* 点击左侧配置名时强制切回配置模式并跳转该配置最新日期分支（日志模式下同样生效，并退出选择模式）

* 更换水印按钮弹出两次文件选择框：容器级委托与手动事件监听双触发，移除手动绑定

* 配置修改相互干扰：更换水印改为局部更新，不再重置其他未保存的修改状态

* 暂停后继续顺序错乱：按暂停时冻结的顺位插回队列并重新编号，暂停任务不再占用排队序号

* 项目切换/展开动画异常：azbar 动画消失、填充动画仅首个项目生效、折叠再展开高亮丢失、切换时残留旧字母等问题逐一修复

* 任务列表边界状态异常：脚本子进程关闭影响队列推进、暂停后队列卡死等修复

### 优化

* 三组切换按钮（中间模式 / 右上 txt / 左上排序）：激活态与悬浮态配色统一（激活品牌蓝实底白字、激活悬浮深蓝、未激活悬浮深灰），随皮肤适配

* azbar 切换动画：收敛为整列从左到右扫描渐显，动画期间字母保持初始状态，结束后单独触发首字母高亮判定（仅点击配置名 / 滚动条移动 / 拖拽字母时触发）

* 样式细节：两个修改警示语样式统一、红色提示框改用不遮挡内部内容的描边实现、滚动条预留槽位、粘性置顶项目名无空隙

* 主窗口「任务」按钮数字改为运行中 + 排队任务数（暂停不计入）；「重置预检测」「刷新配置文件」按钮改用不同图标区分

## 2026-08-24（v1.1 · v1.2）

### 新增功能

* 单实例锁：同一时刻仅允许运行一个主进程实例（跨盘、跨开发版/打包版统一生效），重复打开程序时自动唤出现有实例的主窗口

* 独立设置页面：左下角改为「设置」按钮，打开模态设置窗口（固定尺寸、可通过失焦关闭）；左侧分为「通用设置 / 批量拼接 / 视频复刻」三类，分别管理主题、应用配置与两个脚本的全局参数，所有修改保存至 config.json

* 脚本参数可配置化：成片名中原先的固定名称删除，改为脚本开头可配置参数；成片名中的序号标识提取为全局参数「序号后缀」（默认 YX）并在设置页可调；成片名预览使用真实当天日期

* 脚本启动前参数校验：config.json 缺失必需参数时禁止启动脚本，并在状态栏提示缺失原因

* 系统托盘：点击关闭窗口时最小化到右下角任务栏而非退出进程；托盘菜单含「显示主窗口 / 打开任务列表 / 设置 / 退出」（选项间以分隔线区分），双击托盘图标可唤出主窗口，气泡提示 "Video Lab"

* 图标换新：App 图标以矢量 SVG 设计并生成多尺寸 PNG/ICO 内嵌至 EXE；托盘图标提供 @1.25x / @1.5x / @2x 多分辨率，适配不同缩放的系统任务栏

* 设置窗口交互：打开设置时主窗口显示半透明模糊遮罩（与预检测遮罩一致）；有未保存修改时主窗口失焦不关闭，播放报错音并在状态栏显示红色闪烁提醒「有未保存的修改，请先保存」

* 输入框规范：红色必填 `*` 移入输入框内部右侧固定位置，各输入框尺寸统一

* 重置预检测：左侧新增红色预警样式的「重置预检测」按钮，二次确认后清除预检测缓存并对所有配置指向的路径去重全量探测，过程中显示带转圈与实时进度条的等待窗口

* 日期分支右键菜单：新增「打开文件 / 打开文件夹」，替代原左下角打开按钮

* 「重新检测配置」按钮：由左上角移动至左下角，样式与「保存为当日配置」保持一致

* 日志模式批量复刻：日志底部栏新增「选择」按钮切换复选模式，可多选成片后一键执行「完全复刻 / 去重复刻」；支持全选/全不选并实时显示已选数量；选择模式下片段右侧的单条「复刻」按钮保留，仍可单独复刻

* 配置↔日志精确配对：成片文件夹外的单独配置以 `*` 后缀标示，同一成片的多份配置按 `-1/-2` 序号化，日志与配置按同一成片文件夹、序号与日期精确配对；无对应日志或配置时，「日志 / 配置列表」切换按钮自动禁用并悬浮提示原因

* 复刻模式独立项目：侧栏新增「复刻」项目，含「原片复刻」「去重复刻」两个入口，点击直接进入对应日志列表

* 日志多日期分支：日志视图按配置名聚合展示全部日志日期分支，可在不同日期的日志间切换

### 修复

* 任务列表在已有任务时仍显示「暂无任务」：非空列表渲染前移除占位元素

* 单成片实时进度误判为编码失败：进度输出由 `Write-Output` 改为 `[Console]::Out.WriteLine`，避免被 PowerShell 收集为函数返回值数组

* 日志视图右键日期分支打开的是配置路径：改为指向对应日期的日志文件

* 日志归属与配置不一致：统一按「项目 + 配置名 + 日期分支」判定，修复日志显示无数据/归属错位

* 「重新检测中…」状态不结束：完成后以绿字提示「重新检测完成」，不再被自动重载覆盖

* 设置窗口可被调整大小：改为固定尺寸模态窗口，不可最大化/最小化/调整尺寸

### 优化

* 成片实时进度：进度条由按成片粒度改为单个成片实时推进，上方显示「成片 X/Y · 百分比%」，百分比数字右对齐

* 预检测提速：视频探测改为异步并发并限制并发数，基于文件 mtime+size 指纹缓存未改动结果，冷启动耗时大幅下降；前端加防抖与宽限期，避免遮罩闪烁

* 皮肤切换控件：左下角为调色板图标按钮，点击弹出上拉选择浮层；菜单项以两个并排小方块（背景色 + 主题色）预览，按拼音排序（白蓝→黑橙→灰橙），边框色随皮肤自适应；重新区分黑橙与灰橙配色

* 界面配色变量化：输入框等控件配色统一走 CSS 变量（背景 / 边框 / 聚焦 / 文字）随皮肤适配；「运行/启动脚本」、弹窗主按钮 / 危险按钮移除写死颜色

* 缓存统一：scan 缓存与预检测缓存统一为下划线命名并统一存放到 Cache 子文件夹，启动时自动迁移旧缓存

* 左侧项目列表：默认宽度拉长至 285px，并设为可拖拽调整的最小宽度（只能拉大、不可小于 285px）

* 日志底部栏布局：完全复刻 / 去重复刻按钮分色区分并移至最右下角；选择按钮与片段列表复刻按钮列对齐；右侧细线随预览区宽度拖动实时联动

* 模式切换按钮：改为手机 App 风格滑动开关（长椭圆轨道 + 圆形滑块 + 弹性回弹动画 + 轨道颜色渐变），随皮肤适配；日期分支顶部两个模式按钮及左下角按钮悬浮时变深蓝 + 白字

* 状态栏与标题栏路径显示：选中配置/日志时状态栏显示「已选择:"完整路径"」，右上角标题显示「实时/原始:"相对路径"」（半角冒号），长路径悬浮 title 可查看完整内容；日期分支按钮悬浮显示对应完整路径

* 配置列表展开时项目名粘性置顶，切换项目无需大幅滚动

## 2026-08-23（v1.0）

### 新增功能

* 任务列表窗口：主窗口状态栏右下角新增「任务」按钮（带运行中数量徽标），独立窗口实时查看每个任务的互斥锁状态、进度条与实时日志；排队等待互斥锁的任务可随时停止

* 暂停/继续：排队等待互斥锁的任务可暂停挂起，避免长期占住排队位置

* 任务失败提示：失败时任务窗口保留完整控制台输出，并在任务行左侧用红字显示人类可读的失败原因

* 任务标题规范化：左侧显示项目名（去掉 .txt 后缀），右侧灰底标签保留「批量拼接 / 视频复刻」及去重标识，避免重复

* A-Z 纵向索引条：项目列表按名称排序时显示拼音首字母索引，支持拖动跳转、当前字母放大高亮突出；无匹配字母自动隐藏；名称降序时索引条自动倒转；跳转时目标字母组首个配置置顶，列表不足时释放后回弹

* 皮肤切换：状态栏左下角下拉框，内置 白蓝 / 黑橙 / 灰橙 三套皮肤，切换即生效并持久化

* 日志全局搜索：输入成片名可全局定位到所在日期分支与成片位置，结果高亮 2.5 秒

* 日志片段存在性预检测：点开日志成片时对片段逐一探测，缺失片段路径文字变红、右键「打开文件/文件夹」置灰并显示红色缺失提示

* 日志联动：日志视图右侧实时显示日志内容与行号，点开成片自动跳转并高亮对应行

* 加载遮罩：启动检测、选用路径、重新扫描时居中显示转圈与提示，避免误以为卡死

### 修复

* 组内日期分支按时间排序未生效：新增按分支时间（MMDD）降序排列，名称/时间排序可切换升降序并显示 ▲/▼ 箭头

* 添加路径按钮无反应：Electron 不支持原生提示框，改用系统文件选择框（文件夹 / 视频文件多选）

* 脚本启动「exe 秒退」：脚本调用改为直接以 PowerShell 进程运行，绕开命令中转导致的中文路径乱码

* 右键菜单失效：仅点击菜单外部时关闭，菜单项不再误触发关闭

* 状态栏报「读取配置失败」：搜索片名 / 跳转日志分支时对日志模式下不存在的界面元素做空值保护

### 优化

* 缓存机制：改为基于文件 mtime + 大小生成指纹，未修改文件复用缓存，外部修改文件后启动时自动重算

* 路径、水印、排除字段自动去除首尾引号，避免解析错误

* 片段缺失提示文字移至路径行最右侧，视觉更清晰

## 2026-08-20（v0.1.0）

### 新增功能

* 项目首版：项目/TXT 扫描与版本去重、配置与日志解析、成片预检测、快速复刻、同名文件高亮、成片副本对比、完整路径展示

* 覆盖保护弹窗：保存配置前确认是否覆盖，展示将被覆盖的文件路径

* 复刻双模式：点击复刻弹出选择框，支持「完全复刻」与「去重复刻」，经环境变量交由脚本区分执行

* 批量成片参数处理：分组数为空默认不分组，成片数为空显示红色提醒

* 从 pywebview 架构迁移至 Electron：主进程 / 预加载 / 后端逻辑分层，界面与后端通过安全桥接通信

* 项目更名为 Video Lab，界面、后端与包名统一

* 「外部编辑」移至右侧预览区头部；新增「选择路径」按钮，路径持久化到配置

* 首次运行配置引导：缺失的工作目录 / 脚本目录 / 水印目录按项弹出原生选择框引导配置

* 列表排序：名称默认升序、时间默认降序，点击排序按钮切换升降序

### 修复

* Electron 启动空白窗口：Windows 下直接传应用目录启动，不再因参数丢失落入默认壳

* 路径输入框长按框选文字误触发排序：仅拖拽把手可触发排序

* 弹窗重复打开：重建界面时准确移除旧的事件监听

* 预检测不合格时保存 / 生成按钮置灰禁用，配置名左侧红字提醒

* 路径 / 水印 / 排除字段双引号残留导致解析错误

### 优化

* 排除字段仅保留输入框，回车即添加

* 日志片段右键可打开文件 / 所在路径；日期分支双击打开对应文件夹

* 左侧项目列表、中心↔预览分隔条、预览面板三处宽度均可拖拽调节；预览面板 240–720px

* TXT 默认显示实时预览

* 启动脚本调用方式调整为独立终端窗口运行，进度可见

